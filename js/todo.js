let divListDOM = document.querySelector('#divList');
divListDOM.addEventListener('submit', listHandler);

function listHandler(event) {
    event.preventDefault();
    const LIST_ITEM = document.querySelector('#list');
}